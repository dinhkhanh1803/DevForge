<UserControl x:Class="DevForge.Desktop.Execution.ExecutionCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="Execution Center">
    <Grid Margin="{StaticResource Page.Padding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel>
            <Button DockPanel.Dock="Right"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    AutomationProperties.Name="Cancel execution" />
            <StackPanel>
                <TextBlock Style="{StaticResource PageHeading}" Text="Execution Center" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Status.Glyph}" Margin="0,0,8,0" />
                    <TextBlock Text="{Binding Status.Label}" FontWeight="SemiBold" />
                </StackPanel>
            </StackPanel>
        </DockPanel>
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <ListBox ItemsSource="{Binding Steps}"
                     VirtualizingStackPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,6">
                            <TextBlock>
                                <Run Text="{Binding StatusGlyph}" />
                                <Run Text="{Binding DisplayName}" FontWeight="SemiBold" />
                            </TextBlock>
                            <TextBlock Text="{Binding StatusLabel}" />
                            <TextBlock Text="{Binding ErrorCode}" />
                            <TextBlock Text="{Binding Remediation}" TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ListBox Grid.Column="1"
                     Margin="16,0,0,0"
                     ItemsSource="{Binding ProgressLines}"
                     VirtualizingStackPanel.IsVirtualizing="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Resume" Command="{Binding ResumeCommand}" />
            <Button Content="Retry" Command="{Binding RetryCommand}" Margin="8,0,0,0" />
            <Button Content="Cleanup" Command="{Binding CleanupCommand}" Margin="8,0,0,0" />
            <Button Content="Open Staging" IsEnabled="False" />
            <Button Content="Support Bundle" IsEnabled="False" Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
