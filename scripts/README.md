# Maintainer scripts

This directory is reserved for reviewed, non-interactive maintainer automation. M0 requires no repository script; build and test commands are documented at the repository root and executed directly by CI.

