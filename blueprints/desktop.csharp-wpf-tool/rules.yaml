- id: windows-only
  condition: runtime.os == "windows"
  severity: blocking
  message: This blueprint requires Windows for native WPF compilation.
  remediation: Run DevForge Studio on a supported Windows environment.
  override: none
