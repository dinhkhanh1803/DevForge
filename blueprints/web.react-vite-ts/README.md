# React + Vite + TypeScript blueprint

This built-in package emits a static, locked React project. Generation never invokes a framework scaffolder. Dependency installation uses pnpm frozen-lockfile mode with lifecycle scripts disabled, followed by fixed lint, typecheck, test, and build validators.
