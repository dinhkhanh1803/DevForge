# Validators

Frozen Ruff format/lint, mypy, pytest, package build, CLI help and native desktop
smoke are mandatory. A missing display or Tcl/Tk fails, never skips. Commands run
through the closed IProcessRunner vocabulary; no arbitrary Python evaluation.
