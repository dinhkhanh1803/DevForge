# {{ project.name }}

Native Python Desktop candidate generated by DevForge Studio.

## Packaging
uv run --frozen --no-sync --no-config pyproject-build --no-isolation creates a wheel
and source distribution in dist. These require Python 3.14 with Tcl/Tk and are not
self-contained Windows installers. No automatic deployment or network service exists.

## Release validation
Install the reviewed wheel into a clean compatible environment and run both
entrypoint help and native smoke before distributing. Keep exact wheel hashes
and test evidence. Sign an installer only in a separately reviewed packaging task.
