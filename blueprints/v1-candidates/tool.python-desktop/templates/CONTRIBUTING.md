# {{ project.name }}

Native Python Desktop candidate generated by DevForge Studio.

## Workflow
Use a feature branch and small reviewed commits. Follow the selected repository
branch policy recorded in .devforge/policy.snapshot.json. Run TESTING.md before
review; include regression tests with every reproducible fix.

## Review
Check UI-thread safety, pinned locks, module boundaries and secret-free diffs.
Do not commit .env, caches, local environments or credentials. Run frozen sync
after checkout; never update locks as a side effect of validation.
