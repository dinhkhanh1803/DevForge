# Blueprint packages

This directory is reserved for versioned DevForge blueprint packages. Blueprint implementation starts only in its assigned milestone after the engine and security gates are available; M0 intentionally contains no executable blueprint content.

