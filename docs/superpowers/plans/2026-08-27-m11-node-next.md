# M11 Node boundary and Next candidate implementation plan

> Execute inline using executing-plans and test-driven-development. Preserve the
> existing worktree changes and release checklist; no commit/push in this slice.

**Goal:** Prove the production Node source/tooling boundary before adding one
isolated Next candidate.

**Architecture:** ADR-0028; guarded snapshot and evidence in Infrastructure,
existing IProcessRunner commands, marker-owned staging and unchanged final tree.
**Tech stack:** Native DevForge C#/.NET 10; generated Node/pnpm/Next TypeScript.

## Task 1 — Runtime

Files: `Processes/NodeProcessEnvironment.cs`, `WindowsProcessRunner.cs`,
`TrustedExecutableResolver.cs`, and `WindowsProcessRunnerTests.cs`.

- [x] Add runtime tests using the process helper (`echo-env`) for declared PATH,
  SystemRoot, CI, protected override refusal and absence of ambient variables.
- [x] Run the focused tests and record expected RED before implementing policy.
- [x] Resolve installed pnpm.cjs next to trusted PATH package-manager shims;
  validate Node and script files, never execute the shim or download Corepack.
- [x] Add protected Node policy and run focused integration tests to GREEN.

## Task 2 — Source snapshot and evidence

Files: `Execution/NodeExecutionWorkspace.cs`, `ProcessExecutionHandlers.cs`,
`OwnedStagingWorkspaceManager.cs`, focused Node workspace and handler tests.

- [x] RED tests copy exact source, keep outputs outside payload, reject changed/
  missing source, unexpected files, output namespace collision and reparse points.
- [x] Implement guarded bounded copy using workspace streams and atomic writes;
  compare payload/source snapshot before and after each operation, compute a
  deterministic length-framed source/artifact inventory digest.
- [x] Wire pnpm root commands into tooling workspace, leaving arguments closed.
  Preserve fresh-staging replay and validator revalidation behavior.
- [x] Test marker-owned cleanup with distinct tooling bounds and unchanged target.
- [x] Run existing React real toolchain through the production runner and require
  clean local publication, repeat verification, source tamper rejection.

## Task 3 — Candidate, only after foundation GREEN

Files: `blueprints/v1-candidates/web.next-ts/**`, Next candidate contract/E2E
tests, shared test-only catalog fixture and content links.

- [x] Add missing-candidate RED contract, pin reviewed exact versions and lock,
  create App Router source with strict types and local environment validation.
- [x] Add independent formatter, ESLint, typecheck, tests, build and bounded
  loopback production smoke with timeout/failure-safe shutdown.
- [x] Complete seven handoff files and canonical package checksum inventory.
- [x] Add tamper/quarantine, deterministic planning/source, occupied-target,
  local publication, repeated recovery and source mutation regressions.
- [x] Execute real frozen install and every generated validator, then publication.

## Task 4 — Verification and handoff

- [x] `dotnet restore DevForge.sln --locked-mode --disable-build-servers -m:1`
- [x] `dotnet format DevForge.sln --no-restore --include <changed C# files>`
- [x] Repeat format with `--verify-no-changes` and the same explicit file list.
- [x] `dotnet build DevForge.sln -c Release --no-restore --disable-build-servers -m:1 -p:UseSharedCompilation=false`
- [x] `dotnet test DevForge.sln -c Release --no-build --no-restore -m:1`
- [x] `git diff --check`; review changes against ADR/security requirements.
- [x] Record exact results in status/verification docs; retain external release
  holds. A failed or unavailable gate is not success or a skipped passing test.
